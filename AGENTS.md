# AGENTS.md

## Cursor Cloud specific instructions

This repository (`ilovevideo`) is currently an empty project with only a `README.md`. There are no applications, services, dependencies, or build systems configured yet.

- **No package manager lockfile exists** — when one is added, the update script should be updated accordingly (e.g. `npm install`, `pip install -r requirements.txt`, etc.).
- **No services to start** — once application code is added, document startup commands here.
- **No lint/test/build commands** — these should be documented here once configured.
